# Proof of execution — fixture-repo

Generated by `polyglot-eval generate-data --repo examples/fixture-repo` on the bundled **fixture-repo** (repo-agnostic Python task store).

| Task | Artifact | Status |
|------|----------|--------|
| I1 | `artifacts/I1/data.json` | ER entities scanned from `app/store.py`, `Task`, `TaskStore` |
| I2 | `artifacts/I2/data.json` | Flow trace from `app/main.py` entry handlers |
| I3 | `artifacts/I3/data.json` | Safe-change plan (validation guard) |
| I4 | `artifacts/I4/data.json` | Polyglot pair template |
| I5 | `artifacts/I5/data.json` | Docker strategy from repo `Dockerfile` |
| I6 | `artifacts/I6/data.json` | Bug diagnosis (empty-store edge case) |

Regenerate:

```bash
polyglot-eval generate-data --repo examples/fixture-repo
cp -R examples/fixture-repo/reports examples/proof-of-execution/
```

View locally:

```bash
polyglot-eval serve-ui --task dashboard --repo examples/fixture-repo
```
